apiVersion: 1

deleteDatasources:
  - name: PostgresQL

datasources:
  - name: postgres
    type: PostgreSQL
    access: proxy
    url: http://postgres:5432
    user: postgres
    database: postgres
    version: 1
    editable: true
